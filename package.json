{
  "name": "@ghom/entity-pixi",
  "version": "1.0.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Suitable to be included in a bundle for Node and the browser for entity based apps using pixi.js",
  "prettier": {
    "semi": false
  },
  "jest": {
    "setupFiles": [
      "./tests/setup.js"
    ]
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "build": "tsc",
    "test": "npm run build && jest tests/test.js",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "@types/offscreencanvas": "^2019.6.4",
    "jest": "^27.5.1",
    "prettier": "^2.6.1",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@ghom/entity": "^1.0.18",
    "canvas": "^2.9.1",
    "gl": "^5.0.0",
    "pixi.js": "^6.3.0"
  }
}
