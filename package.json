{
  "name": "@ghom/entity-base",
  "version": "1.0.13",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "description": "TypeScript only package for entity based app",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "test": "tsc --noEmit",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "@ghom/event-emitter": "^1.0.3"
  }
}
