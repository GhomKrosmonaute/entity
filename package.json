{
  "name": "@ghom/entity-p5",
  "version": "1.2.1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Suitable to be included in a bundle for Node and the browser for entity based apps using p5",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "build": "tsc",
    "test": "npm run build && jest test.js",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "prettier": "^2.6.0",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "@ghom/entity": "^1.0.17",
    "@types/p5": "^1.4.1",
    "p5": "^1.4.1"
  }
}
