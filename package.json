{
  "name": "@ghom/entity",
  "version": "1.0.0",
  "license": "MIT",
  "main": "src/index.ts",
  "description": "TypeScript only package for entity based app using p5",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "test": "tsc --noEmit",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "@types/p5": "^1.4.0",
    "p5": "^1.4.1"
  }
}
