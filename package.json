{
  "name": "@ghom/entity",
  "version": "1.0.14",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Suitable to be included in a bundle for Node and the browser for entity based apps",
  "prettier": {
    "semi": false
  },
  "scripts": {
    "format": "prettier --write src tsconfig.*",
    "build": "tsc",
    "test": "npm run build && jest test.js",
    "prepublishOnly": "npm run format && npm test"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "typescript": "^4.5.5",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "@ghom/event-emitter": "^1.0.4"
  }
}
